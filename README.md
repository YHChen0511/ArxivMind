<div align="center">

# ArxivMind - AI 驱动的深度学术调研助手

**从想法到论文，只需一步。**

</div>

---

## 📖 项目简介

**ArxivMind** 是一个智能化的学术调研工具，旨在解决传统关键词搜索的局限性。它利用大语言模型（LLM）的推理能力，深入理解研究者的 **Idea**，自动将其转化为专业的学术搜索策略，并对海量 Arxiv 论文进行智能重排序。

无论你是寻找灵感的博士生，还是探索新方向的研究员，ArxivMind 都能帮你快速锁定最相关的核心文献。

## ✨ 核心功能

1. 🧠 **意图分析 (Intent Analysis)**
    * 不只是匹配关键词，而是理解你的“研究意图”。
    * 自动识别核心技术挑战（Core Challenge）和技术路线（Technical Route）。
    * 将模糊的想法转化为 3 个不同维度的精准 Arxiv 查询语句。

2. 🔍 **多路召回 (Multi-Query Retrieval)**
    * 并行执行多个搜索查询，最大化论文召回率。
    * 自动去重，确保获取最广泛的相关文献集合。

3. 📊 **智能重排序 (Reasoning Rerank)**
    * **Relevance First**: LLM 阅读论文摘要，判断其是否解决了你的核心问题。
    * **Recency Boost**: 优先推荐 2024-2025 年的最新成果，同时保留经典奠基性工作。
    * 提供每篇论文的 **推荐理由** 和 **相关性打分**。

4. 📝 **策略分析报告**
    * 生成搜索策略分析，解释为什么选择这些方向。
    * 提供领域关键词趋势和建议的后续探索方向。

## 🛠️ 技术栈

* **Frontend**: React, TypeScript, Vite, Tailwind CSS
* **Backend**: Python, FastAPI
* **LLM Service**: OpenAI 兼容接口 (支持 Gemini, GPT-4 等)
* **Data Source**: Arxiv API

## 🚀 快速开始

### 前置要求

* **Node.js** (v16 或更高版本)
* **Python** (v3.8 或更高版本)
* **API Key**: 一个兼容 OpenAI 格式的 API Key (如 Gemini, GPTGod 等)

### 1. 后端设置 (Python)

后端负责处理复杂的逻辑推理和 Arxiv 数据获取。

```bash
# 1. 进入项目根目录
cd arxivmind

# 2. 创建并激活虚拟环境 (推荐)
uv sync

# 3. 启动后端服务
uv run main.py
```

> **注意**: 后端服务默认运行在 `http://0.0.0.0:8000`。
> 请确保 main.py 中的 `api_key` 和 `base_url` 已正确配置（建议后续改为环境变量）。

### 2. 前端设置 (React)

前端提供直观的用户交互界面。

```bash
# 1. 安装依赖
npm install

# 2. 配置环境变量
# 复制 .env.local 示例并填入你的 API Key (如果前端也需要直接调用 LLM)
# 或者确保 vite.config.ts 配置正确

# 3. 启动开发服务器
npm run dev
```

> 前端页面默认运行在 `http://localhost:3000`。

## ⚙️ 配置说明

### 后端 API 配置

目前 main.py 中使用了硬编码的 API Key。为了安全起见，建议在生产环境中使用环境变量。

你可以在 main.py 中修改 `client` 的初始化方式：

```python
# main.py
import os

client = OpenAI(
    api_key=os.getenv("LLM_API_KEY"),
    base_url=os.getenv("LLM_API_BASE_URL", "https://api.openai.com/v1")
)
```

### 端口配置

* **Frontend**: 修改 vite.config.ts 中的 `server.port`。
* **Backend**: 修改 main.py 底部的 `uvicorn.run(..., port=8000)`。
